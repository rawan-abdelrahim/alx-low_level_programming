#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _memset -Entry point
 *
 * @s: is pointer
 *
 * @b: is input char
 *
 * @n: is input number
 *
 * Return: Always 0 (Success)
*/
char *_memset(char *s, char b, unsigned int n)
{
        return (0);
}
